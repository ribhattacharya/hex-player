#include "../../include/computer_strategy/RandomMove.hpp"
#include <utility>

IntPair RandomMove::calculateMove(const Board& board,
                                              PlayerIDEnum playerId) {
    return std::make_pair(0, 0);
}